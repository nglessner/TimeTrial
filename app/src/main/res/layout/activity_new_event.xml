<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.nglessner.timetrial.NewEventActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/startEventButtonText"
        android:id="@+id/startEventButton"
        android:layout_alignParentStart="true"
        android:onClick="startEvent" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/addRiderButtonText"
        android:id="@+id/addRiderToEvent"
        android:layout_alignTop="@+id/startEventButton"
        android:layout_centerHorizontal="true"
        android:onClick="addRiderToEventDialog" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/EventRiderList"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/startEventButton" />

</RelativeLayout>